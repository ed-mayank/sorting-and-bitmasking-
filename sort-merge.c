#include <stdio.h>
int main()
{
    int n,i;
    printf("How many numbers do you want to sort : ");
    scanf("%d",&n);

    int A[n];
    printf("Enter %d numbers: ",n);
    for(i=0; i<n; i++)
    scanf("%d",&A[i]);

    
}